For running the present file, it is necesary to install the libraries rtb, pt3d, using the current codes on the terminal:

Roboticstoolbox:

pip3 install roboticstoolbox-python

Pytransform3D:

pip install pytransform3d